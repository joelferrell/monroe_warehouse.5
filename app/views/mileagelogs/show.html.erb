<%- model_class = Mileagelog -%>
<div class="page-header">
  <h1><%=t '.title', :default => Mileagelog %></h1>
</div>

<div class="fieldset">
  <dl>
      <dt class="label label-default"><%= model_class.human_attribute_name(:starting_mile) %></dt>
      <dd><pre class="prettyprint"><%= @mileagelog.starting_mile %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:ending_mile) %></dt>
      <dd><pre class="prettyprint"><%= @mileagelog.ending_mile %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:rate) %></dt>
      <dd><pre class="prettyprint"><%= @mileagelog.rate %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:total_miles) %></dt>
      <dd><pre class="prettyprint"><%= @mileagelog.total_miles %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:mile_total_cost) %></dt>
      <dd><pre class="prettyprint"><%= @mileagelog.mile_total_cost %></pre></dd>
  </dl>
</div>

<div class="form-group">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              mileagelogs_path, :class => 'btn btn-default'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_mileagelog_path(@mileagelog), :class => 'btn btn-default' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              mileagelog_path(@mileagelog),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>

